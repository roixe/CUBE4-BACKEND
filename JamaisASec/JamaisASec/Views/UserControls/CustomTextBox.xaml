<UserControl x:Class="JamaisASec.Views.UserControls.CustomTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="200" Background="White" x:Name="customTextBox">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVis"/>
    </UserControl.Resources>
    <StackPanel Margin="10,5,10,5">
        <TextBlock Text="{Binding ElementName=customTextBox, Path=Caption}" Foreground="Gray" FontSize="13" Margin="0 0 0 8"/>
        <Grid>
            <TextBlock Margin="13 0" VerticalAlignment="Center" Foreground="{StaticResource LightGray}" IsHitTestVisible="False"
                       Panel.ZIndex="1" FontSize="14" Text="{Binding ElementName=customTextBox, Path=Hint}"
                       Visibility="{Binding ElementName=textBox, Path=Text.IsEmpty, Converter={StaticResource BooleanToVis}}"/>
            <TextBox x:Name="textBox" Text="{Binding ElementName=customTextBox, Path=Text, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        <TextBlock Text="{Binding ElementName=customTextBox, Path=ErrorMessage}" 
                   Foreground="Red" FontSize="11" Margin="5,3,0,0"
                   Visibility="{Binding ElementName=customTextBox, Path=ErrorMessageVisibility}"/>
    </StackPanel>
</UserControl>
