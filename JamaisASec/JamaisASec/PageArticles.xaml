<Page x:Class="JamaisASec.PageArticles"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:local="clr-namespace:JamaisASec"
      xmlns:userControls="clr-namespace:JamaisASec.UserControls"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="av"
      Title="ProduitsPage" av:DesignHeight="484.51" av:DesignWidth="850.816">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Boutons de navigation -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="5">
            <Button x:Name="ShowArticlesButton" Content="Articles" Style="{StaticResource tabButton}"/>
            <Button x:Name="ShowFamillesButton" Content="Familles" Style="{StaticResource tabButton}"/>
        </StackPanel>

        <!-- ContentControl -->
        <ContentControl Grid.Row="1" x:Name="ContentArea"/>
    </Grid>
</Page>
