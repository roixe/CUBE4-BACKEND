<Page x:Class="JamaisASec.PageStocks"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" 
      Title="ProduitsPage" Background="White" av:DesignHeight="484.51" av:DesignWidth="850.816">

    <Grid Margin="10">

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,10">
            <Button Content="Rechercher par nom de produit" Width="190" Margin="5" />
        </StackPanel>


        <Border BorderBrush="Black" BorderThickness="1" Margin="0,50,0,0" Padding="5">
            <DataGrid AutoGenerateColumns="False" HeadersVisibility="Column"
                      Background="White" Foreground="Black" GridLinesVisibility="All">

                <DataGrid.Columns>
                    
                        <DataGridTemplateColumn Header="Nom" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="Produit Test" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <DataGridTemplateColumn Header="Description" Width="4*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="Description de test" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Stock" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="-" Width="20" Margin="2"
                                            Click="DecrementStock_Click"/>
                                    <TextBlock Text="10" Width="40" TextAlignment="Center" VerticalAlignment="Center"/>
                                    <Button Content="+" Width="20" Margin="2"
                                            Click="IncrementStock_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="Familles" Binding="{Binding Famille}" Width="2*"/>
                    
                </DataGrid.Columns>
                <TextBlock Text="Test" Margin="5" Grid.Row="1" Grid.Column="0"/>
            </DataGrid>
        </Border>
    </Grid>
    
</Page>
